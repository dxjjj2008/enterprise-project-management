# 企业项目管理系统 - 环境变量配置
# Enterprise Project Management System - Environment Variables

# ===========================================
# 后端配置 (Backend Configuration)
# ===========================================

# 数据库连接URL
# 开发环境使用 SQLite: sqlite:///./app.db
# 生产环境使用 PostgreSQL: postgresql://user:password@host:port/dbname
DATABASE_URL=sqlite:///./data/app.db

# JWT 密钥 (生产环境必须修改！)
# 使用以下命令生成强密钥: openssl rand -hex 32
SECRET_KEY=your-secret-key-change-in-production

# JWT 算法
ALGORITHM=HS256

# 访问令牌过期时间 (分钟)
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Redis 缓存 URL (可选)
# REDIS_URL=redis://localhost:6379/0

# 日志级别 (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# ===========================================
# 前端配置 (Frontend Configuration)
# ===========================================

# API 基础 URL
# 开发环境: http://localhost:8000
# 生产环境: https://your-domain.com/api
VITE_API_BASE_URL=http://localhost:8000

# ===========================================
# 生产环境额外配置 (Production Only)
# ===========================================

# 是否启用 HTTPS
# ENABLE_HTTPS=true

# SSL 证书路径
# SSL_CERT_PATH=/path/to/cert.pem
# SSL_KEY_PATH=/path/to/key.pem

# 域名配置
# DOMAIN=your-domain.com

# 管理员邮箱
# ADMIN_EMAIL=admin@example.com
