apiVersion: apps/v1
kind: Deployment
metadata:
  name: epms-frontend
  labels:
    app: epms-frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: epms-frontend
  template:
    metadata:
      labels:
        app: epms-frontend
    spec:
      containers:
      - name: frontend
        image: epms-frontend:latest
        ports:
        - containerPort: 80
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 10
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: epms-frontend-service
spec:
  selector:
    app: epms-frontend
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
  type: ClusterIP
