# 任务看板详细设计文档

**版本**: v1.0
**创建日期**: 2026-02-08
**状态**: 设计中

---

## 目录

1. [概述](#1-概述)
2. [页面布局](#2-页面布局)
3. [组件设计](#3-组件设计)
4. [数据模型](#4-数据模型)
5. [交互设计](#5-交互设计)
6. [API 接口](#6-api-接口)
7. [状态管理](#7-状态管理)
8. [样式规范](#8-样式规范)
9. [响应式设计](#9-响应式设计)
10. [错误处理](#10-错误处理)

---

## 1. 概述

### 1.1 文档目的

本文档描述任务看板页面的详细设计方案，包括 UI 布局、组件结构、数据模型、交互逻辑等，为开发人员提供完整的技术指导。

### 1.2 术语定义

| 术语 | 定义 |
|------|------|
| 看板 | Kanban 视图，以列的形式展示任务状态 |
| 任务卡片 | 看板中的最小单元，代表一个任务 |
| 拖拽 | 用户通过鼠标/触摸移动任务的行为 |
| 列 | 按任务状态划分的垂直区域 |

### 1.3 参考文档

- [需求文档](/docs/requirements/2026-02-08-requirements.md)
- [UI/UX 设计规范](/docs/plans/2026-02-08-ui-ux-design.md)
- [系统架构设计](/docs/plans/2026-02-08-project-management-system-design.md)

---

## 2. 页面布局

### 2.1 整体结构

```
┌─────────────────────────────────────────────────────────────────┐
│  任务看板                                                          │
├─────────────────────────────────────────────────────────────────┤
│  [项目选择器 v]  [+ 新建任务]                              [筛选] │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐                              │
│  │ 草稿    │ │ 进行中   │ │ 已完成   │                              │
│  │ 3/12    │ │ 5/12    │ │ 4/12    │                              │
│  ├─────────┤ ├─────────┤ ├─────────┤                              │
│  │ ┌─────┐ │ │ ┌─────┐ │ │ ┌─────┐ │                              │
│  │ │任务 │ │ │ │任务 │ │ │ │任务 │ │                              │
│  │ │卡片 │ │ │ │卡片 │ │ │ │卡片 │ │                              │
│  │ └─────┘ │ │ └─────┘ │ │ └─────┘ │                              │
│  │ ┌─────┐ │ │ ┌─────┐ │ │ ┌─────┐ │                              │
│  │ │任务 │ │ │ │任务 │ │ │ │任务 │ │                              │
│  │ │卡片 │ │ │ │卡片 │ │ │ │卡片 │ │                              │
│  │ └─────┘ │ │ └─────┘ │ │ └─────┘ │                              │
│  └─────────┘ └─────────┘ └─────────┘                              │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 页面尺寸规范

| 区域 | 高度 | 说明 |
|------|------|------|
| 页面头部 | 56px | 包含标题、操作栏 |
| 列头部 | 48px | 标题 + 任务数量 |
| 列间距 | 16px | 列之间的水平间距 |
| 列内边距 | 12px | 列内容的内边距 |
| 卡片间距 | 8px | 卡片之间的垂直间距 |

### 2.3 看板列规范

#### 2.3.1 列定义

| 列 ID | 标题 | 状态值 | 背景色 | 说明 |
|-------|------|--------|--------|------|
| draft | 草稿 | draft | `#FAFAFA` | 新创建未提交的任务 |
| in_progress | 进行中 | in_progress | `#E6F4FF` | 正在执行的任务 |
| done | 已完成 | done | `#F6FFED` | 已完成的任务 |

> **说明**：采用简化的三状态流程（草稿 → 进行中 → 已完成），符合敏捷开发实践。

#### 2.3.2 列头部样式

```
┌─────────────────────────┐
│  [图标] 待办      3/12 │  ← 列标题 + 任务数量
└─────────────────────────┘
```

- 标题字体：14px，600 粗细
- 数量：12px 灰色，圆形背景

### 2.4 任务卡片规范

#### 2.4.1 卡片尺寸

- 最小高度：80px
- 最大高度：200px（超出滚动）
- 宽度：100% 自适应列宽

#### 2.4.2 卡片结构

```
┌─────────────────────────────────────┐
│  [优先级] 任务标题               [···]│  ← 操作按钮
├─────────────────────────────────────┤
│  [标签]                             │
├─────────────────────────────────────┤
│  👤 张经理    📅 02/15       💬 3  │  ← 负责人、截止日期、评论数
└─────────────────────────────────────┘
```

#### 2.4.3 优先级标签

| 优先级 | 颜色 | 标签文字 |
|--------|------|---------|
| high | `#FF4D4F` | 紧急 |
| medium | `#FAAD14` | 中 |
| low | `#52C41A` | 低 |

---

## 3. 组件设计

### 3.1 组件树

```
Board (页面根组件)
├── BoardHeader (页面头部)
│   ├── BoardTitle (标题)
│   └── BoardActions (操作栏)
│       ├── ProjectSelector (项目选择器)
│       ├── CreateTaskButton (新建任务按钮)
│       └── FilterDropdown (筛选下拉)
├── BoardBody (看板主体)
│   └── BoardColumn (看板列) × 3
│       ├── ColumnHeader (列头部)
│       │   ├── ColumnTitle (列标题)
│       │   └── ColumnCount (任务数量)
│       └── TaskList (任务列表)
│           └── TaskCard (任务卡片) × N
│               ├── TaskCardHeader (卡片头部)
│               ├── TaskCardBody (卡片内容)
│               └── TaskCardFooter (卡片底部)
└── TaskDetailDialog (任务详情弹窗)
    ├── DialogHeader (弹窗头部)
    ├── DialogBody (弹窗内容)
    │   ├── TaskInfo (任务信息)
    │   ├── SubtaskList (子任务列表)
    │   └── CommentSection (评论区)
    └── DialogFooter (弹窗底部)
```

### 3.2 组件职责

#### 3.2.1 Board

**职责**：
- 页面根组件
- 管理看板整体状态
- 处理全局事件

**Props**：
```typescript
interface Props {
  projectId?: string  // 当前选中的项目 ID
}
```

**Events**：
| 事件名 | 参数 | 说明 |
|--------|------|------|
| task-create | task | 创建任务 |
| task-update | task | 更新任务 |
| task-delete | taskId | 删除任务 |

#### 3.2.2 BoardColumn

**职责**：
- 渲染单个看板列
- 管理列内的任务列表
- 处理拖拽放置

**Props**：
```typescript
interface Props {
  column: ColumnConfig  // 列配置
  tasks: Task[]         // 该列的任务列表
}
```

**Events**：
| 事件名 | 参数 | 说明 |
|--------|------|------|
| task-drop | { task, fromColumn, toColumn } | 任务拖拽放置 |

#### 3.2.3 TaskCard

**职责**：
- 渲染任务卡片
- 显示任务基本信息
- 处理点击事件

**Props**：
```typescript
interface Props {
  task: Task  // 任务数据
}
```

**Events**：
| 事件名 | 参数 | 说明 |
|--------|------|------|
| click | task | 点击卡片 |
| drag-start | task | 开始拖拽 |
| drag-end | task | 结束拖拽 |

### 3.3 组件通信

```
┌─────────────┐
│   Board     │  ← 状态中心
└──────┬──────┘
       │ 提供/更新状态
       ▼
┌─────────────┐     ┌─────────────────┐
│BoardColumn  │ ←→ │   TaskCard      │
└─────────────┘     └─────────────────┘
       │ 拖拽事件
       ▼
┌─────────────────┐
│TaskDetailDialog │
└─────────────────┘
```

---

## 4. 数据模型

### 4.1 任务数据结构

```typescript
interface Task {
  id: number                    // 任务 ID，唯一标识
  projectId: number             // 所属项目 ID
  title: string                // 任务标题，必填
  description: string          // 任务描述，可选
  status: TaskStatus          // 任务状态
  priority: TaskPriority       // 优先级
  assigneeId?: number          // 负责人 ID
  assignee?: User              // 负责人信息
  reporterId: number           // 报告人 ID
  dueDate?: string             // 截止日期 (YYYY-MM-DD)
  tags: string[]              // 标签列表
  subtasks: Subtask[]         // 子任务列表
  comments: Comment[]         // 评论列表
  attachments: Attachment[]   // 附件列表
  createdAt: string          // 创建时间
  updatedAt: string          // 更新时间
}

type TaskStatus = 'draft' | 'in_progress' | 'done'

type TaskPriority = 'high' | 'medium' | 'low'

interface Subtask {
  id: number
  title: string
  completed: boolean
}

interface Comment {
  id: number
  userId: number
  user: User
  content: string
  createdAt: string
}

interface Attachment {
  id: number
  name: string
  url: string
  size: number
  type: string
}
```

### 4.2 看板列配置

```typescript
interface ColumnConfig {
  id: string
  title: string
  status: TaskStatus
  color: string
  bgColor: string
}
```

### 4.3 用户数据结构

```typescript
interface User {
  id: number
  name: string
  avatar: string
  email: string
}
```

### 4.4 Mock 数据示例

```javascript
const mockTasks = [
  {
    id: 1,
    projectId: 1,
    title: '完成用户认证模块',
    description: '实现登录、注册、找回密码功能，包括 JWT Token 验证',
    status: 'in_progress',
    priority: 'high',
    assignee: { id: 1, name: '张经理', avatar: '...' },
    dueDate: '2026-02-15',
    tags: ['开发', '后端'],
    subtasks: [
      { id: 1, title: '设计数据库', completed: true },
      { id: 2, title: '编写API', completed: false },
      { id: 3, title: '单元测试', completed: false }
    ],
    comments: [
      { id: 1, userId: 2, user: { id: 2, name: '李开发', avatar: '...' }, content: 'API已提交测试', createdAt: '2026-02-08 10:30' }
    ],
    attachments: [],
    createdAt: '2026-02-01 08:00',
    updatedAt: '2026-02-08 10:30'
  },
  {
    id: 2,
    projectId: 1,
    title: '设计前端页面',
    description: '完成所有页面的 UI 设计和原型图',
    status: 'draft',
    priority: 'medium',
    assignee: { id: 2, name: '李开发', avatar: '...' },
    dueDate: '2026-02-20',
    tags: ['设计', '前端'],
    subtasks: [],
    comments: [],
    attachments: [],
    createdAt: '2026-02-02 09:00',
    updatedAt: '2026-02-02 09:00'
  },
  {
    id: 3,
    projectId: 1,
    title: '数据库优化',
    description: '对慢查询进行优化，添加必要的索引',
    status: 'done',
    priority: 'low',
    assignee: { id: 1, name: '张经理', avatar: '...' },
    dueDate: '2026-02-10',
    tags: ['优化', '数据库'],
    subtasks: [
      { id: 1, title: '分析慢查询', completed: true },
      { id: 2, title: '添加索引', completed: true }
    ],
    comments: [],
    attachments: [],
    createdAt: '2026-02-03 14:00',
    updatedAt: '2026-02-08 16:00'
  }
]

const columnConfigs = [
  { id: 'draft', title: '草稿', status: 'draft', color: '#8C8C8C', bgColor: '#FAFAFA' },
  { id: 'in_progress', title: '进行中', status: 'in_progress', color: '#1E5EB8', bgColor: '#E6F4FF' },
  { id: 'done', title: '已完成', status: 'done', color: '#52C41A', bgColor: '#F6FFED' }
]
```

---

## 5. 交互设计

### 5.1 页面加载

| 场景 | 行为 | 加载状态 |
|------|------|---------|
| 首次加载 | 显示骨架屏 | 加载中 |
| 数据加载完成 | 显示看板内容 | 加载完成 |
| 加载失败 | 显示错误提示 | 错误状态 |

### 5.2 拖拽交互

#### 5.2.1 拖拽流程

```
1. 用户按住任务卡片
   ↓
2. 卡片显示阴影效果
   ↓
3. 用户拖动到目标列
   ↓
4. 目标列高亮显示
   ↓
5. 用户释放鼠标
   ↓
6. 任务移动到目标列
   ↓
7. 显示更新成功提示
```

#### 5.2.2 拖拽规则

| 规则 | 说明 |
|------|------|
| 列间拖拽 | 任务可以在任意列间移动 |
| 列内排序 | 任务可以在同一列内调整顺序 |
| 即时更新 | 拖拽完成后立即更新状态 |
| 撤销支持 | 拖拽后可撤销操作 |

### 5.3 点击交互

| 元素 | 点击行为 | 触发动作 |
|------|---------|---------|
| 任务卡片 | 单击 | 打开任务详情弹窗 |
| 新建任务按钮 | 单击 | 打开新建任务弹窗 |
| 列头部 | 单击 | 无操作（仅显示统计） |
| 项目选择器 | 单击 | 显示项目列表 |

### 5.4 键盘快捷键

| 快捷键 | 功能 | 说明 |
|--------|------|------|
| N | 新建任务 | 打开新建任务弹窗 |
| / | 聚焦搜索框 | 快速搜索任务 |
| Esc | 关闭弹窗 | 关闭当前打开的弹窗 |

### 5.5 动画效果

| 场景 | 动画类型 | 时长 |
|------|---------|------|
| 任务移动 | 平滑过渡 | 200ms |
| 卡片进入 | 淡入效果 | 300ms |
| 卡片离开 | 淡出效果 | 200ms |
| 弹窗打开 | 缩放效果 | 250ms |

---

## 6. API 接口

### 6.1 接口列表

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/v1/projects/{id}/tasks | 获取项目任务列表 |
| POST | /api/v1/tasks | 创建任务 |
| GET | /api/v1/tasks/{id} | 获取任务详情 |
| PUT | /api/v1/tasks/{id} | 更新任务 |
| DELETE | /api/v1/tasks/{id} | 删除任务 |
| PUT | /api/v1/tasks/{id}/status | 更新任务状态 |

### 6.2 接口详情

#### 6.2.1 获取任务列表

**请求**：
```http
GET /api/v1/projects/1/tasks?status=draft
Authorization: Bearer <token>
```

**响应**：
```json
{
  "code": 200,
  "data": {
    "tasks": [
      {
        "id": 1,
        "title": "完成用户认证模块",
        "status": "draft",
        "priority": "high",
        ...
      }
    ],
    "total": 12,
    "page": 1,
    "pageSize": 50
  }
}
```

#### 6.2.2 创建任务

**请求**：
```http
POST /api/v1/tasks
Authorization: Bearer <token>
Content-Type: application/json

{
  "projectId": 1,
  "title": "新任务",
  "description": "任务描述",
  "status": "draft",
  "priority": "medium",
  "assigneeId": 1,
  "dueDate": "2026-02-20"
}
```

**响应**：
```json
{
  "code": 200,
  "data": {
    "id": 100,
    "title": "新任务",
    ...
  }
}
```

#### 6.2.3 更新任务状态

**请求**：
```http
PUT /api/v1/tasks/1/status
Authorization: Bearer <token>
Content-Type: application/json

{
  "status": "in_progress"
}
```

**响应**：
```json
{
  "code": 200,
  "message": "状态更新成功"
}
```

---

## 7. 状态管理

### 7.1 本地状态

使用 Vue `ref`/`reactive` 管理：

```typescript
// 看板列配置
const columns = ref<ColumnConfig[]>([...])

// 任务列表
const tasks = ref<Task[]>([])

// 当前选中项目
const currentProjectId = ref<number>(1)

// 加载状态
const loading = ref(false)

// 错误信息
const error = ref<string | null>(null)
```

### 7.2 计算属性

```typescript
// 按状态分组任务
const tasksByColumn = computed(() => {
  return columns.value.map(column => ({
    ...column,
    tasks: tasks.value.filter(task => task.status === column.status)
  }))
})

// 任务统计
const taskStats = computed(() => {
  return {
    total: tasks.value.length,
    draft: tasks.value.filter(t => t.status === 'draft').length,
    inProgress: tasks.value.filter(t => t.status === 'in_progress').length,
    done: tasks.value.filter(t => t.status === 'done').length
  }
})
```

### 7.3 方法定义

```typescript
// 加载任务
const loadTasks = async (projectId: number) => {
  loading.value = true
  error.value = null
  try {
    const response = await fetchTasks(projectId)
    tasks.value = response.data.tasks
  } catch (e) {
    error.value = '加载任务失败'
  } finally {
    loading.value = false
  }
}

// 创建任务
const createTask = async (taskData: Partial<Task>) => {
  const response = await createTask(taskData)
  tasks.value.unshift(response.data)
}

// 更新任务
const updateTask = async (task: Task) => {
  const response = await updateTask(task.id, task)
  const index = tasks.value.findIndex(t => t.id === task.id)
  if (index !== -1) {
    tasks.value[index] = response.data
  }
}

// 删除任务
const deleteTask = async (taskId: number) => {
  await deleteTask(taskId)
  tasks.value = tasks.value.filter(t => t.id !== taskId)
}

// 移动任务
const moveTask = async (taskId: number, newStatus: TaskStatus) => {
  const task = tasks.value.find(t => t.id === taskId)
  if (task) {
    task.status = newStatus
    await updateTask(task.id, { status: newStatus })
  }
}
```

---

## 8. 样式规范

### 8.1 颜色变量

```css
:root {
  /* 品牌色 */
  --primary-color: #1E5EB8;
  --primary-light: #E6F4FF;
  
  /* 状态色 */
  --success-color: #52C41A;
  --success-bg: #F6FFED;
  --warning-color: #FAAD14;
  --warning-bg: #FFFBE6;
  --danger-color: #FF4D4F;
  --danger-bg: #FFF2F0;
  
  /* 中性色 */
  --text-primary: #262626;
  --text-secondary: #595959;
  --text-tertiary: #8C8C8C;
  --border-color: #E8E8E8;
  --bg-color: #FAFAFA;
}
```

### 8.2 字体规范

```css
.board-title {
  font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
  font-size: 18px;
  font-weight: 600;
  color: var(--text-primary);
}

.column-title {
  font-family: 'Inter', sans-serif;
  font-size: 14px;
  font-weight: 600;
  color: var(--text-primary);
}

.task-title {
  font-family: 'Inter', sans-serif;
  font-size: 14px;
  font-weight: 500;
  color: var(--text-primary);
}
```

### 8.3 间距规范

```css
.board {
  padding: 24px;
}

.column {
  margin-right: 16px;
  padding: 12px;
}

.task-card {
  margin-bottom: 8px;
  padding: 12px;
  border-radius: 8px;
}
```

---

## 9. 响应式设计

### 9.1 断点定义

| 断点 | 屏幕宽度 | 列数 |
|------|---------|------|
| xs | < 576px | 1 列 |
| sm | ≥ 576px | 2 列 |
| md | ≥ 768px | 2 列 |
| lg | ≥ 992px | 3 列 |
| xl | ≥ 1200px | 3 列 |

### 9.2 响应式规则

```css
.board {
  display: flex;
  gap: 16px;
  overflow-x: auto;
}

.column {
  flex: 0 0 300px;
}

@media (max-width: 768px) {
  .column {
    flex: 0 0 280px;
  }
  
  .task-card {
    padding: 10px;
  }
}
```

### 9.3 移动端适配

- 禁止横向滚动（使用单指滑动）
- 卡片宽度 100%
- 隐藏非必要操作按钮
- 简化弹窗内容

---

## 10. 错误处理

### 10.1 错误类型

| 错误类型 | 处理方式 | 用户提示 |
|---------|---------|---------|
| 网络错误 | 重试机制 | "网络异常，请重试" |
| 权限错误 | 跳转登录 | "请先登录" |
| 验证错误 | 表单提示 | 显示具体字段错误 |
| 服务器错误 | 全局提示 | "服务异常，请稍后重试" |

### 10.2 错误边界

```vue
<template>
  <div class="board">
    <template v-if="error">
      <el-result
        icon="error"
        :title="error"
        sub-title="请刷新页面重试"
      >
        <template #extra>
          <el-button type="primary" @click="loadTasks">刷新</el-button>
        </template>
      </el-result>
    </template>
    <template v-else>
      <BoardColumn ... />
    </template>
  </div>
</template>
```

---

## 附录

### A. 依赖清单

| 依赖 | 版本 | 用途 |
|------|------|------|
| Vue | 3.x | 框架 |
| Element Plus | 2.x | UI 组件库 |
| sortablejs | 1.x | 拖拽功能 |
| dayjs | 1.x | 日期处理 |

### B. 参考资源

- [Element Plus Draggable](https://element-plus.org/zh-CN/component/draggable.html)
- [SortableJS 文档](https://sortablejs.github.io/Sortable/)
- [Kanban Design Patterns](https://uxdesign.cc/kanban-design-patterns)

---

**文档版本**: v1.0
**最后更新**: 2026-02-08
**作者**: AI Assistant
